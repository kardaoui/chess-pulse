name: chesspulse
version: '1.0.0'
config-version: 2

profile: chesspulse

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  chesspulse:
    staging:
      +schema: staging
      +materialized: view
    mart:
      +schema: mart
      +materialized: table